Unix and Linux are the key things that we should be mastered as a tester. Unix commands must be memorized and it takes a lot of practice to be mastered. Unix has many useful commands such as mkdir to create a directory, touch to create a file, cat to read a file, vi to add content, mv to move file, etc

